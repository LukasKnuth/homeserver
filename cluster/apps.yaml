apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: 5m # todo increase?
  timeout: 5m
  dependsOn:
  - infrastructure
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./cluster/apps
  prune: true # todo needed?
  wait: true # todo needed?